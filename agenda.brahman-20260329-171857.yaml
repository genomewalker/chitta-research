# Generated by Brahman
# Rationale: The DiscussionRoom synthesis bugs are now fully characterized but unfixed — continuing to probe them yields no new information. The highest-leverage next move is to follow the downstream consequence: corrupted claims entering the belief graph. Fixing claim integrity (error taint + deduplication) unblocks all other open questions by making the graph trustworthy enough to build on, and it converts the existing cluster of synthesis findings into actionable design requirements rather than dead-end observations.
programs:
  - title: "Belief Graph Integrity: Deduplication and Error-Claim Separation"
    domain: chitta-research / software_engineering
    questions:
      - "How are hypotheses currently matched for deduplication across sessions — exact string, embedding similarity, or structural hash?"
      - "Does the belief graph ingest claims derived from error/panic responses, and if so how are they flagged or weighted?"
      - "What schema changes to the claim representation would enable reliable deduplication and error-taint tracking?"
    methods:
      - "Read and trace the belief graph ingestion path in the Rust codebase from DiscussionRoom output to stored claim"
      - "Grep for deduplication logic (fuzzy match, cosine threshold, or hash-based) in chitta-field and cr-agents"
      - "Instrument a test run that deliberately injects an error-response claim and observe whether it persists in the graph"
    priority: 1.0
    max_budget_usd: 10.0

budget:
  total_usd: 10.0
  gpu_slots: 0
  cpu_workers: 4

llm:
  provider: room
  model: "Belief Graph Integrity: Deduplication and Error-Claim Separation"

chitta:
  mind_path: ~/.claude/mind
