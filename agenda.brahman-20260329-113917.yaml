# Generated by Brahman
# Rationale: These three experiments were identified in prior cycles and not executed. They require no new infrastructure, have binary or enumerable outcomes, and together close the panic-scope confidence gap, bound the NaN attack surface at the correct layer, and audit the validity of prior experimental output. Proposing benchmark harnesses or factorial LLM designs before these run is premature. Crate complexity is a deterministic measurement answerable with one CLI invocation and should be removed from the open-questions backlog. DiscussionRoom operationalization can proceed after the three minimal experiments run, with automated proxy metrics replacing blinded human scoring to avoid the evaluator-availability and blinding problems.
programs:
  - title: "Execute the three identified minimal experiments before any infrastructure work"
    domain: software_engineering
    questions:
      - "Does a Tokio-spawned task panic terminate the process or silently drop the task, as measured by exit code and process survival under the exact runtime configuration used in this project — not by source reading?"
      - "Do serde_yaml and the YAML parser in use normalize .NaN, .NAN, and -.nan to the same f32::NAN before serde deserialization, or do any variants reach downstream code as distinct bit patterns — measured at the parser layer, not the YAML spec?"
      - "What is the proximate error type of each of the 17 failed runs grouped by crate, and does any group share the same tooling-artifact flaw as the retracted claim?"
    methods:
      - "Write a minimal Tokio binary targeting the project's exact Tokio version and runtime flavor (current_thread vs multi_thread): spawn a task that panics after 10ms; record exit code, process survival, and whether the parent runtime continues. Declare the falsifier in advance: if process-level abort is not observed, the current panic-scope claim must be downgraded from 0.90."
      - "Write a serde_yaml round-trip test with inputs '.NaN', '.NAN', '-.nan', 'null', '.inf', '-.inf'; assert which produce f32::NAN via is_nan() and which produce distinct bit patterns via f32::to_bits(); record exact serde_yaml and yaml-rust versions. The real attack surface boundary is confirmed or falsified at this layer, not at the YAML spec."
      - "Query all 17 failed run records directly; extract the error variant not the message; count occurrences by crate and error type; flag any that match the symbol-search-on-wrong-target pattern from the retracted claim. Do not generate new hypotheses until the audit is complete and the scope of the tooling-artifact contamination is known."
    priority: 1.0
    max_budget_usd: 10.0

budget:
  total_usd: 10.0
  gpu_slots: 0
  cpu_workers: 4

llm:
  provider: room
  model: "Execute the three identified minimal experiments before any infrastructure work"

chitta:
  mind_path: ~/.claude/mind
