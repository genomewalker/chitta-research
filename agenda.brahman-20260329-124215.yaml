# Generated by Brahman
# Rationale: None of the three open gaps can be honestly closed by running experiments in their current form: the DiscussionRoom baseline is unfalsifiable without a pre-registered metric, the panic test targets a synthetic proxy, and the YAML tests are spec-version ambiguous. One session spent on pre-registration, Cargo.lock inspection, and tooling-artifact audit removes the structural causes of stagnation and ensures the next round of experiments produces claims that can actually move confidence above or below 0.90 based on evidence rather than confirmation.
programs:
  - title: "Pre-Register Metrics and Fix Test-Target Specificity Before Running DiscussionRoom, Panic, and YAML Baselines"
    domain: software_engineering
    questions:
      - "What is the single pre-registered primary metric for DiscussionRoom compute-parity (correct unique claims per unit cost, latency, or token count) and what numeric threshold constitutes parity vs. non-parity?"
      - "Which serde_yaml version is pinned in Cargo.lock, and do the .NaN/.NAN/.nan/-.nan test cases require YAML 1.1 or 1.2 float-scalar semantics?"
      - "Does the actual Hotr spawned task in cr-agents survive a NaN priority sort under the real daemon runtime configuration — specifically asserting JoinHandle outcome, sibling task completion, shared-state integrity, process exit code, and successful completion of a subsequent healthy request?"
      - "Are there f32 or f64 fields in serde-derived config structs other than ProgramConfig.priority that can receive NaN or infinity from YAML input or programmatic construction?"
      - "How many prior 'not found' negative results were tooling artifacts vs. true absences when re-audited via direct grep with explicit file paths independent of symbol-search tooling?"
    methods:
      - "Read Cargo.lock to extract the exact serde_yaml version and consult its changelog to determine applicable YAML spec and float-scalar rules before writing any deserialization test"
      - "Pre-register one primary metric with a numeric threshold for DiscussionRoom parity before collecting any baseline data — this single step converts the baseline from an exploration into a falsifiable experiment"
      - "Write the panic-containment fault-injection test against the actual daemon entrypoint runtime (not a synthetic tokio::spawn), asserting shared-state integrity post-panic and successful subsequent-request handling; include a positive-control test that intentionally propagates failure across task boundaries to prove the harness can distinguish contained from uncontained panics"
      - "Build a table-driven YAML scalar matrix covering .nan/.NaN/.NAN/-.nan/.inf/-.inf/quoted variants/invalid near-misses, asserting both parse acceptance/rejection and downstream ranking behavior, pinned to the serde_yaml version confirmed from Cargo.lock"
      - "Grep all serde-derived structs for f32/f64 fields to enumerate the full NaN attack surface beyond ProgramConfig.priority"
      - "Re-audit all prior 'not found' symbol-search results using direct file grep with line numbers to produce a true-negative vs. artifact ratio independent of the potentially scoped symbol-search tool"
    priority: 1.0
    max_budget_usd: 10.0

budget:
  total_usd: 10.0
  gpu_slots: 0
  cpu_workers: 4

llm:
  provider: room
  model: "Pre-Register Metrics and Fix Test-Target Specificity Before Running DiscussionRoom, Panic, and YAML Baselines"

chitta:
  mind_path: ~/.claude/mind
