# Generated by Brahman
# Rationale: These are the cheapest experiments that falsify the most load-bearing assumptions in the current belief graph. The compute-parity test directly challenges the DiscussionRoom architectural bet without circular self-evaluation. The panic recovery test closes the open gap in the Hotr safety claim that static analysis cannot close. The field-tracing test bounds actual attack surface before further fuzzing investment. The rerun test measures whether the belief graph is accumulating reliable knowledge or reproducibility noise. All are executable against the current codebase without new benchmark infrastructure, breaking the stagnation by generating new discriminating evidence rather than additional methodology proposals.
programs:
  - title: "Minimal Falsifying Tests: Compute-Parity DiscussionRoom Baseline, Panic Recovery, and Config Attack Surface"
    domain: software_engineering
    questions:
      - "Does DiscussionRoom produce higher claim precision than a single-model call with an equal total token budget (N_agents × M_rounds × avg_tokens_per_turn), not equal prompt length?"
      - "Under repeated rapid panics in the same Tokio task holding shared state, does the Hotr executor restart cleanly, drop the task silently, or deadlock?"
      - "Which ProgramConfig fields beyond priority reach shell execution or file I/O, and which are validated post-deserialization versus accepted raw?"
      - "What is the inter-run agreement rate for top claims when the same hypothesis is re-tested twice against the same frozen repo snapshot?"
      - "Does feeding .NaN, .NAN, -.nan, null, and empty string to each numeric ProgramConfig field at runtime produce the behavior inferred from static analysis?"
    methods:
      - "Compute-matched DiscussionRoom baseline: single model call with total_tokens equal to full DiscussionRoom budget; run on 5 held-out architecture questions; score claim precision manually against source code ground truth; report win/loss/tie with effect size."
      - "Write a test harness that triggers repeated panics in a spawned Tokio task holding a shared mutex; assert task restarts without deadlock and shared state is consistent post-recovery; treat absence of assertion as a falsification of the existing safety claim."
      - "Static grep for all ProgramConfig field usages; trace each field to its first consumer; flag any reaching Command::new, std::fs, or path construction without validation; produce an inventory before further fuzzing."
      - "Rerun the top 5 existing hypotheses on the same repo snapshot; compute Jaccard similarity of claims produced across runs; report as a reliability coefficient to calibrate belief graph confidence values."
      - "Instrument serde deserialization of ProgramConfig with unit tests feeding adversarial numeric values; assert on actual runtime behavior, not inferred behavior from static path tracing alone."
      - "Pre-register all success thresholds before executing comparisons: e.g., DiscussionRoom must achieve ≥20% reduction in unsupported claims to justify its token cost; panic recovery must pass 100 consecutive adversarial cycles without deadlock."
    priority: 1.0
    max_budget_usd: 10.0

budget:
  total_usd: 10.0
  gpu_slots: 0
  cpu_workers: 4

llm:
  provider: room
  model: "Minimal Falsifying Tests: Compute-Parity DiscussionRoom Baseline, Panic Recovery, and Config Attack Surface"

chitta:
  mind_path: ~/.claude/mind
